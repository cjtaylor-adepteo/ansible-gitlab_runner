#!/sbin/openrc-run

name=gitlab-runner
supervisor=supervisor-daemon
command="{{ gitlab_runner_link }}"
command_args=" $GITLAB_RUNNER_ARGS"
command_background=true
pidfile="/var/run/gitlab-runner.pid"

depend() {
    need net
}
